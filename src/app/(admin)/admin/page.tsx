/**
 * Admin Dashboard Page
 *
 * TODO: When admin features are requested, implement them here
 *
 * Examples of admin features you might add:
 * - User management (list, edit, delete users)
 * - Content moderation
 * - Analytics dashboard
 * - System settings
 * - Database management
 *
 * This page is separate from the main app layout.
 * You can create a custom admin layout in /src/app/admin/layout.tsx if needed.
 */

export default async function AdminPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-4xl font-bold mb-4">Admin Dashboard</h1>
        <p className="text-gray-600">
          Admin features will be implemented here when requested.
        </p>
      </div>
    </div>
  )
}
